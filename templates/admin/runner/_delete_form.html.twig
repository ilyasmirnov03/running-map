<form method="post" action="{{ path('app_admin_runner_delete', {'id': runner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ runner.id) }}">
    <button class="btn">Delete</button>
</form>
