<form method="post" action="{{ path('app_admin_run_delete', {'id': run.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ run.id) }}">
    <button class="btn">Delete</button>
</form>
